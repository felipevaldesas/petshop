<div class="jumbotron">
  <center>
    <h1><%= @pet.name %></h1>
    <img src="<%= @pet.image %>" alt="Animalandia shop">
  </center>
  <h3>Family: <%= @pet.family %></h3>  
  <h3>Costo: <%= @pet.cost %></h3>
</div>

<%= link_to 'Edit', edit_pet_path(@pet) %> |
<%= link_to 'Back', pets_path %>
